# /analyze-kpi — KPI 현황 분석 및 원인 진단

**실행:** kpi-analysis → tnps-prediction (T-NPS 미달 시) → quality-trend (QA 미달 시)

**사용법:**
```
/analyze-kpi [월간/주간 KPI 데이터]
/analyze-kpi target [목표 달성 시뮬레이션]
```

**다음 커맨드 제안:** `/predict-tnps` → `/weekly-report` → `/set-targets`
