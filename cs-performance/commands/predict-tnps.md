---
description: T-NPS 예측 및 리스크 상담사 식별
argument-hint: [상담/KPI 데이터]
---

# /predict-tnps — T-NPS 예측

**실행:** tnps-prediction → agent-benchmarking (리스크 상담사 식별)

**사용법:**
```
/predict-tnps [상담/KPI 데이터]
/predict-tnps risk  ← 리스크 상담사 집중 식별
```

**다음 커맨드 제안:** `/coach` → `/analyze-kpi`
