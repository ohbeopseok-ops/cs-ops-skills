---
description: 데이터 기반 목표치 설정
argument-hint: [현재 실적 데이터]
---

# /set-targets — 목표치 설정

**실행:** kpi-analysis (현황 파악) → target-setting

**사용법:**
```
/set-targets [현재 실적 데이터]
/set-targets stretch  ← 도전 목표 포함 3단계 설정
```

**다음 커맨드 제안:** `/plan-okr` → `/benchmark`
