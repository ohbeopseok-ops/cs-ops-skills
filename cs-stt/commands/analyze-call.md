---
description: STT 텍스트 전체 분석 (품질+감성+준수율)
argument-hint: [STT 텍스트]
---

# /analyze-call — STT 전체 분석

**실행:** pii-detection → conversation-analysis → compliance-check → qa-scoring

**사용법:**
```
/analyze-call [STT 텍스트]
/analyze-call elderly [고령자 상담]
/analyze-call risk  ← 리스크 신호 집중 분석
```

**다음 커맨드 제안:** `/coach` → `/evaluate`
